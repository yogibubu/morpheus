[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "matrix-engines"
version = "0.1.0rc5"
description = "Backend engine wrappers for MATRIX"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

